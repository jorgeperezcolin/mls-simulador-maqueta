# Paquete de agentes del simulador MLS (versión maqueta).
# Aquí se definirán clases específicas como ClienteAgent, CompetenciaAgent, etc.
#
# En la maqueta actual no se instancian agentes uno por uno,
# pero esta estructura permite escalar a la versión multiagente real.

from .base_agent import BaseAgent
